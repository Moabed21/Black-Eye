# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
blackeye

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.out

# Dependency directories (errors the compiler avoids if these exist)
vendor/

# Configuration and data generated by BlackEye
audit.log
config.toml

# IDEs and editors
.idea/
.vscode/
*.swp
*.swo
.DS_Store
